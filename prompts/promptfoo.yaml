prompts:
  - Tell me more about start-up tax exemption
  - Add a test trigger prompt   # optional dummy prompt to trigger workflow

providers:
  - openai:gpt-3.5-turbo  # or ollama:gemma:2b

tests:
  - vars:
      topic: entrepreneur
    assert:
      - type: contains
        value: period of time
      - type: javascript
        value: output.length < 1000  # Keep it short and sweet
