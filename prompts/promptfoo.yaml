prompts:
  - Tell me more about start-up tax exemption
  - Add a test trigger prompt   # optional dummy prompt to trigger workflow

providers:
  - ollama:gemma:2b  # or ollama:gemma:2b

tests:
  - vars:
      topic: entrepreneur
    assert:
      - type: contains
        value: period of time
      - type: javascript
        value: output.length < 2000  # Keep it short and sweet
